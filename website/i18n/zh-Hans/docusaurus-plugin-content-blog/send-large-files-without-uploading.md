---
slug: send-large-files-without-uploading
title: "如何不上传云端发送大文件"
description: "了解如何不经任何服务器上传发送大文件。P2P 直连 — 无云、无大小限制。免费试用 EteDrop。"
authors: [etedrop]
tags: [large-files, p2p, privacy]
date: 2026-06-01
---

每次上传到云端，都是两趟路：你的设备 → 服务器，再服务器 → 收件人。5 GB 文件在整个链路上相当于消耗约 10 GB 带宽，且文件还躺在别人的服务器上。

EteDrop — 为直连而生 — 把路程砍成一趟：从你的设备直达收件人设备。中间无服务器、无云端上传、无第三方存储。

## 上传的问题

云端分享的标准流程：

1. 上传到其服务器
2. 服务器暂存或长期保存
3. 分享下载链接
4. 收件人从服务器下载

可行，但有代价：**时间**（两趟）、**隐私**（文件在别人硬件上）、**大小上限**（免费档常限 2 GB）、**留存**（可能比预期存更久）。

## 什么是 P2P 文件传输？

点对点（P2P）在两台设备间直传，无中间服务器、无云端中继。EteDrop 使用 WebRTC — 与现代浏览器视频通话同源，内置于各主流浏览器。无需插件、扩展；接收端零安装。

## 三步不上传发送（3 步）

**第 1 步：打开 EteDrop**

在任意现代浏览器打开 [EteDrop](/down)。手机、平板或电脑均可。无需登录。

**第 2 步：选文件并分享链接**

选择要发的文件。EteDrop 生成链接与取件码。通过短信、邮件、Slack 等发给收件人。取件码确保只有对的人能访问。

**第 3 步：收件人预览并下载**

收件人在浏览器打开链接、输入取件码、预览 PDF/图片/视频/音频/代码，再下载。

完毕。无上传、无云、无服务器存储。

*进阶：* 同网 LAN 模式自动检测；单次会话可传多个文件。详见 FAQ。

## 为何 P2P 更快：一趟 vs 两趟

云端：设备 → 云 → 收件设备（两趟）。P2P：设备 → 收件设备（一趟）。同网 LAN 接近硬件上限；公网仍省去云端中继。

大文件时一趟差异明显。5 GB、50 Mbps：云端约 14 分钟（上传+下载），P2P 直连约 7 分钟（视网络而定）。

## 架构层面的隐私

云端靠加密与政策承诺隐私。EteDrop 靠架构：文件不抵达任何服务器；信令只帮设备找到彼此后即退出。除预期收件人外，无人能在服务器侧接触文件。

任意大小 — 仅受连接与浏览器限制。10 GB+ 建议稳定有线或强 Wi-Fi；中断需重传 — 目前不支持断点续传。P2P 需双方在线 — 无异步。

## 何时云端仍合理

- 收件人暂不在线（异步）
- 需长期存、多人反复下载
- 需详细传输分析与审计

## 常见问题

**收件人要安装吗？** 否。浏览器打开链接即可。

**有大小限制吗？** 无人工上限，仅受连接与浏览器稳定性约束。

**中断怎么办？** 需重新开始，目前不支持断点续传。

**能一次发多个吗？** 可以，收件人可分别预览与下载。

**真的私密吗？** WebRTC 直连，无服务器存文件；信令不见文件内容；传输完成后不保留文件元数据。

**不上传任何人的服务器发送大文件。** [免费试用 EteDrop →](/down)
